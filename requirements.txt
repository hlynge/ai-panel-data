# ─── Core data handling ────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
pyarrow>=14.0          # Parquet support for pandas
openpyxl>=3.1          # Excel (.xlsx) read/write

# ─── HTTP / config ────────────────────────────────────────────────────────────
requests>=2.31
PyYAML>=6.0

# ─── OECD data ────────────────────────────────────────────────────────────────
# The OECD SDMX API is accessed via raw requests (no extra package needed).
# Optionally install the sdmx1 package for more advanced SDMX parsing:
# sdmx1>=2.14

# ─── World Bank ───────────────────────────────────────────────────────────────
wbgapi>=1.0.12          # Official World Bank Python client

# ─── IMF ──────────────────────────────────────────────────────────────────────
# IMF DataMapper REST API is accessed via raw requests (no extra package needed).

# ─── Country code harmonisation ───────────────────────────────────────────────
country_converter>=1.2   # Converts between country name formats
pycountry>=23.12         # ISO 3166-1 country code validation

# ─── Jupyter (optional, for explore.ipynb) ────────────────────────────────────
# Uncomment if you want to run the exploration notebook:
# jupyter>=1.0
# matplotlib>=3.7
# seaborn>=0.13
# plotly>=5.18
